# SecureGenAI Document Assistant — Python Dependencies
#
# DoD/Compliance Note: Pin ALL versions. Unpinned dependencies mean
# your build today might differ from your build tomorrow. In a STIG'd
# environment, every dependency needs a known version for vulnerability
# scanning (Trivy, Snyk) and SBOM generation.

# ── Web Framework ─────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-multipart==0.0.12        # Required for file uploads in FastAPI

# ── AWS SDK ───────────────────────────────────────────────────────
boto3==1.35.74
botocore==1.35.74

# ── Configuration ─────────────────────────────────────────────────
pydantic==2.10.2
pydantic-settings==2.6.1

# ── Document Processing ──────────────────────────────────────────
pypdf==5.1.0                    # PDF text extraction (pure Python, no C deps)

# ── Observability ────────────────────────────────────────────────
prometheus-client==0.21.0       # Prometheus metrics exposition

# ── Testing (dev only — not installed in production container) ────
# pytest==8.3.4
# pytest-asyncio==0.24.0
# httpx==0.27.2                 # Async test client for FastAPI
# moto==5.0.22                  # Mock AWS services for unit tests
